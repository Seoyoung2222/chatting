server:
  port: 8080

spring:
  application:
    name: chatting
  jackson:
    default-property-inclusion: non_null

api:
  talkplus:
    url: https://api.talkplus.io/v1.4/api
    app-id: ${APP-ID}
    api-key: ${API-KEY}